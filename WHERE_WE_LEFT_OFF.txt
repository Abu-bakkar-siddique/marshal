- Project name: marshal

- Current stack:
  Python
  PySide6
  SQLite

- Repo/package state:
  App name stays `marshal`
  Internal Python package is `marshal_app` to avoid conflict with Python's built-in `marshal` module
  Local dev database path defaults to `.marshal-data/marshal.db`
  Local virtualenv exists at `.venv`

- What currently works:
  Create project
  Sidebar project selection
  Add project task
  Mark task done
  Completed tasks move into completed section
  Delete task
  Progress bar updates
  Seed demo data command exists
  Keyboard-first queue controls exist

- Current keyboard queue controls:
  `Ctrl+T` focuses the project task queue
  `Up` / `Down` moves selection
  `Ctrl+D` toggles move mode
  While move mode is on, `Up` / `Down` reorders the selected queued task
  `Esc` exits move mode

- Important commands:
  Run app:
  `cd /home/siddique/Desktop/marshal && source .venv/bin/activate && marshal`

  Reseed demo data:
  `cd /home/siddique/Desktop/marshal && source .venv/bin/activate && marshal-seed-demo`

- Demo data status:
  Demo seeder was added and used
  It populates 3 projects, 17 tasks, and 2 checkpoints

- Main files touched recently:
  `src/marshal_app/ui/main_window.py`
  `src/marshal_app/ui/project_view.py`
  `src/marshal_app/ui/widgets/task_list_widget.py`
  `src/marshal_app/ui/widgets/task_card.py`
  `src/marshal_app/ui/widgets/progress_header.py`
  `src/marshal_app/ui/widgets/completed_panel.py`
  `src/marshal_app/ui/theme/styles.py`
  `src/marshal_app/dev/seed_demo.py`

- Where we left off functionally:
  Mouse drag reorder was abandoned
  Reordering is now keyboard-centric
  The immediate next feature is checkpoint enforcement when leaving the current incomplete active task by reorder

- Where we left off visually:
  You called out UI/design problems, especially the bright blue focus/selection behavior and noisy styling
  I removed obvious visual noise and attempted to suppress native Qt blue highlight
  You need to reopen the app and confirm whether any ugly blue/focus artifact is still visible
  If it is still visible, the next step is to remove that exact remaining Qt focus/selection source before doing more feature work

- Known unfinished features:
  Checkpoint prompt on active-task switch
  Sections for contiguous neighboring tasks
  Standalone task persistence and UI
  Project close flow
  General UI polish pass after core behavior stabilizes

- Important product rules already decided:
  Active task is derived, not stored
  Active task = first incomplete task in a project queue
  Sections are organizational only
  Sections can only wrap contiguous neighboring tasks
  Task order defines priority
  New project tasks go to the bottom
  Completed tasks move to completed section
  Standalone tasks are secondary and more flexible

- What to do first when resuming:
  Run the app
  Check whether the queue focus styling still has any ugly blue or other bad visual state
  If yes, fix that before adding checkpoint logic
  If no, move directly into checkpoint enforcement for reorder-based active-task switches
